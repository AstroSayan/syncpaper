# Git Configuration & Remote User Rule

- Always use the remote user **`AstroSayan`** (or `astrosayan`) for all Git operations, repository URLs, and remote origins:
  - SSH: `git@github.com:AstroSayan/<repo>.git`
  - HTTPS: `https://github.com/AstroSayan/<repo>.git`
- Git commit author identity should always default to:
  - `user.name`: `AstroSayan`
  - `user.email`: `astrosayan@users.noreply.github.com`
